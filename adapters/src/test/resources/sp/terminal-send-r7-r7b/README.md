# R7-R7B frontend compatibility fixtures

Generated by proleap-poc `ca9d0119d2742cf8254107ef55b5d6a743ccc6ff` (SP2.44 only for SEND_TERMINAL), Java21, fixed-source format, 2GiB heap. Each JSON is the real `cobol-semantic-product.json` for its same-named source. COPY lookup uses source/cpy. The generation was run twice with identical bytes.

These final products do not contain terminal SEND ordinary qualification. Source topology remains UNKNOWN_LOCAL; EXECUTABLE_ONLY remains limited; BOUNDED_POSITIVE preserves independent publication. Wire mutations live separately in TerminalSendSuite and never alter these source products.

Storage first-loss: the real CardDemo group FROM has no published logical group view or materialized AIR object. This suite qualifies syntax/transport and the stop boundary, not executable SEND or outer-level dispatch.
