# Validation before readiness fixtures
Frozen bytes generated by the real frontend at the manifest pin. Sources extend the R6 context-resumes witness with a READ FILE statement and, by case, HANDLE ABEND CANCEL / ABEND. The historical source has neither new family. They are paired semantic cases, not identity-equivalent documents. SourceMap/identities and topology remain frontend-owned.

Wire mutations are made only in ValidationBeforeReadinessSuite: contradictory CICS conditions, CALL continuation, and READ target mode. These are distinct from the original producer bytes. No frozen historical fixture or oracle was changed.
