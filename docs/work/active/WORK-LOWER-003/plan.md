# Plano CP0

1. Reconciliar merge real de WORK-LOWER-002; arquivar pacote; nova autorização em WORK-LOWER-003.
2. FREEZE dos oracles, limites, pins e gates; bootstrap Java21/upstream exato em /tmp isolado.
3. RED focal; provar compatibilidade Publication real/codec antes de implementar writer e CLI.
4. Suite output em Maven e obrigatória em semantic/full/CI, sem promover transport/integration:
   catálogo transport exige reader independente, fora deste checkpoint que usa o codec compartilhado.
5. Architecture source/bytecode/Maven/ownership; challenges restaurados e segundo GREEN.
6. Docs, full, scope, diff-check, self-review, certify, commit, verify-commit, push, PR único;
   vincular PR real, repetir gates necessários e consultar checkpoint no head exato (espera 1800s).
7. Parar para review humano; nunca merge/auto-merge, 2B ou E2E.
