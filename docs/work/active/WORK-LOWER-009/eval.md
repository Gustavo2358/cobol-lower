# Evaluation

Production Java/POM/fixture bytes must match base fe7e6ef. CP3 has Return; scalar has one Object/Cell and Assign literal PROGA plus Return and PARTIAL. The 10k shared-DATA fixture must retain all 10000 Assigns and one Object/Cell under default shared codec, round-trip byte-exact and succeed when former 16MiB bound is exceeded. Explicit low codec/validator budgets must fail closed with RESOURCE_LIMIT before file I/O, preserving old destination and creating no fresh output. Full retains every existing suite/challenge; no semantic oracle is relaxed.

Closeout oracle: a valid documentary child selects the certified parent in historical mode on branch and main. Modified production, certificate, source lock, contract or unrelated docs, wrong work identity, active residue and wrong PR must reject. Normal commits keep execution mode.
