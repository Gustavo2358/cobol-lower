# Eval

A synthetic Git fixture creates a base, certified feature head and merge commit.
Git ancestry, trailer resolution and reading committed certificate bytes remain
real; only GitHub responses are controlled. CP0 with null PR resolves the exact
head in historical mode and leaves the record unmodified. Known correct PR
still passes. Wrong explicit PR, later CP/remediation with null PR, wrong branch,
repository/base, missing/ambiguous/unmerged PR, wrong merge/event SHA, unrelated
head and altered merge evidence reject. Branch execution remains distinct.

Freeze the initial test before implementation. Restore the old comparison and
bypass each critical guard in isolated scripts; require focal AssertionError,
exact restoration and second GREEN. Existing runtime fixtures, production
sources, source lock, historical evidence and tests remain byte-identical.
Run docs, architecture, semantic, performance, Git, harness-tests, all challenges
and full; full or CI not executed or failed is never PASS.
