# CP6 W1C contract

Input authority: proleap-poc W1A merge 53d774026a1e4bcd969c7783a1d277aaa87b5f2f, docs/domain/call-semantic-product.md and scalar-text-move.md (read in full). AIR transport authority: air-java W1B PR9 real merge 2a37f5e980ba25fdc79614a66030a84d8bf5b8c9, docs/engineering/air-json.md and docs/quality/cp6-w1b-invoke.md (read). AIR semantic authority remains the normative pin in sources.lock.json.

SPECIFICATION_GUARANTEED: SP target sum, scalar whole-item proof, literal logical value, fitted result, explicit normal continuation, UNKNOWN effects and OPEN outcomes. Lower consumes these facts; written text, PIC, programPoint and array order never supply missing semantics.

Admit a known primary zero-arity entry and linear root chain with zero or more supported MOVEs, exactly one supported CALL, and its explicit normalContinuation pointing to terminal GOBACK. Exactly one CALL is this semantic slice, not a general cardinality budget. Decode all facts before rejecting unsupported forms. Invalid identities/coherence yield INVALID_INPUT; missing essential input/continuation yields BLOCKED_LOWERING; unsupported capability yields UNSUPPORTED_SLICE; operational exhaustion remains IMPLEMENTATION_LIMIT.

Translate DATA once with ScalarDataTranslator. Consume FULL_IDENTITY literal or FITTED_TEXT.textAdjustment.result verbatim; never compute padding. Assign origin combines statement/source/target/adjustment derivation. CALL DATA maps to ComputedTarget(Read(ObjectPlace(selected DATA))); literal maps logical text to LiteralTarget. Category program, namespace cobol.program designate a source reference, not a runtime load module. No tracing prior MOVEs.

Split into two sequences: pre-CALL Assigns and Invoke terminator; explicit normal destination is the real GOBACK Return sequence. Known empty external signature, no arguments/results/effectOperands, UnknownName and UnknownContract with explicit uncertainties. Reads/writes WithinMemory(AllMemory(publication,true)); mustOverwrite empty. Normal successor plus WithinControl(AllControl(publication)); never pure or closed-normal-only. Partial global coverage and open dimension claims preserve uncertainty. Distinct operation/target/read/place/continuation origins and Statement/Operand/Entry correlations.

Build statement/data indices once; walk explicit continuation chain with visited set. Finite O(D+S+R+P+B) time and O(D+S+R+P) space (amortized map lookup; B hashed text); no scan per CALL, timestamp, random ID or temporary-path-derived identity. Revision includes typed CALL target, continuation, surface, knowledge and fitted value.

Excluded: USING, RETURNING/GIVING, handlers, ambiguous/unresolved/refmod/subscript, unsupported MOVE/truncation, nonlinear control, incomplete inputs. No AIR normative changes, FitText, runtime-name canonicalization, dataflow, CFG or dependency facts. I-56 remains a semantic obligation with STRUCTURALLY_VALID/SUCCESS, including after AirJson round-trip. Stop if exact upstream facts/transport cannot support this slice without these exclusions.
