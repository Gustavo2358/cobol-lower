# Evaluation

Freeze expected from SP public contract and AIR model, with no private maps/IDs,
AST/source reconstruction or human log parsing as the semantic oracle. Join using
public statement/operand/entry/data correlations and explicit IDs only.

Five independent RED stages: unsupported real SP1.4; IF not materialized;
typed IF refused by linear admission; admitted plan lacks Branch/Jump;
storage evidence not emitted as premise. Preserve each cause independently.

Closed/open oracles check destinations, arm MOVE order/provenance/fitting,
shared CALL merge, explicit GOBACK, Unknown BOOL with ordered knownReads and
VALUES uncertainty, NoMemory remaining reads, exact ordered proof members/authority/
origin. Round-trip equals and byte-identical re-encode; Validator obligations kept.
Permute sequence array, statement array, rename IDs contrary to lexical order,
and add extra proof member. Negative proof/shape/entry/membership/completion cases
must reject. W1 literal/computed CALL and SP1.3 goldens remain unchanged.

Local mutation campaign includes every 26 W2B countercase from user request;
CI orchestration tests include all 10 required countercases plus aliases.
Full qualification retains historical challenges, performance and real producer
closed/open repeated twice. Fast CI uses a fixed reviewed focal suite and never
executes product mutation/performance/cumulative/real producer qualification.
