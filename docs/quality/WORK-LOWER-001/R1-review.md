# R1 — revisão técnica do delta após review humano

Tipo: self-review, mesmo contexto root implementador; não declarar review independente. Base CP5 `5a00a152d3c05793dad825c16226867c493ee31b`; candidato identificado exclusivamente por `R1.json#/candidate_diff_sha256`, incluindo revisão documental dos dois arquivos excluídos.

Finding 1: dependências active/history resolvem WORK/CP/path/branch/PR no Git, exigem certificado, trailer, conteúdo commitado, parent/digest/FREEZE e único commit alcançável. O receipt é consultado só depois, inclusive PR. Receipt coerente green de outro commit e receipt de outro PR rejeitam. Não há fallback por receipt/latest commit. Ausência de Git/commit válido não passa. CP5 foi reconfirmado remotamente e seu receipt posterior é novo arquivo, sem editar seu certificado.

Finding 2: v2 separa reference_only de candidate_immutable. Expected, golden, source lock, fontes/testes semânticos e contracasos R1 estão enumerados como imutáveis; comparação vale também no commit, não só no snapshot histórico. Documento histórico pode evoluir. Gate de execução rejeita downgrade para v1; auditoria read-only dos certificados v1 permanece sem alegação retroativa. O contrato/oracle textual e os 15 testes iniciais antecederam as correções; testes posteriores ampliaram contracasos de identidade, metadata e certificação, sem alterar expected para acomodar código.

Finding 3: pares históricos válidos e observações estruturadas não concedem autorização. Execução depende de grant v2 explícito e PR OPEN; merged rejeita inclusive grant R1. Auditoria histórica permite OPEN/MERGED por identidade/ancestralidade; reconciliação separada detecta registro open obsoleto perante metadata merged e review de outro SHA. Fixtures GitHub são sintéticas, não afirmações sobre o PR real. Request changes fica no histórico, sem active/CP6. Novo commit de remediação usa trailer próprio e preserva todas as condições de certificação, regressão e falsificação anteriores.

Finding 4: push `**`, sem branch de work item; head/evento/repository validados, trabalho usa evidence própria, main associa merge ao PR por API e ancestralidade. Full inclui certificação publicada; check continua checkpoint/workflow/github-actions/push/SHA exato. Testes cobrem outra branch/WORK sintéticos, main reconciliável read-only, associação ausente/ambígua/SHA incorreto. Histórico não permite nova implementação e CI não presume sucesso do próprio run. Main real ainda não executado: merge não autorizado nesta sessão.

AGENTS: apenas missão/estado e disponibilidade dos gates corrigidos; regras de autorização, backlog e falha de executor preservadas. Sem modelo AIR duplicado, parser/resolução, heurística semântica, gap convertido em sucesso ou expansão de `minimal-entry-goback@1`. Mudança em `run.semantic` remove identidade de WORK hardcoded e mantém exigência dos dois módulos/contagem não-zero. Fixtures antigas mantêm expected; agora copiam Git para provar dependências reais offline. Mocks de full isolam autoridade, que possui positivos/negativos próprios.

Revisão integral do diff desde CP5: scopes só harness/workflow/documentação afetada; arquivos core/adapters/POM/source lock/certificados comparados byte a byte com CP5, sem delta. Mutações A–E e as dez documentais/arquiteturais e três semânticas/custo executam em cópias isoladas, com RED por assertion/causa esperada e restauração/hash/segundo GREEN. Nenhum resíduo entra no candidato. Logs e evidence não incluem credenciais. Certificação final/push/CI precisam dos resultados factuais de R1, não desta narrativa.

Limites preservados: v1 sem prova retrospectiva de imutabilidade; G-DOCS offline não prova GitHub; reconciliação remota é explícita; CI main requer merge que preserve ancestralidade/evidence, não squash/rebase presumidos. Perfil full continua o perfil implementado do primeiro slice, sem release pipeline. Nenhum review humano final, merge, auto-merge ou backlog subsequente foi iniciado pelo agente.
